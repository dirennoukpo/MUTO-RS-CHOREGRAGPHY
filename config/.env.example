# ROS networking defaults
ROS_DOMAIN_ID=33

# Display forwarding (Linux host)
DISPLAY=:0

# Optional host identity overrides for image build args.
# Leave as-is in most cases and rely on Makefile exported values.
HOST_UID=1000
HOST_GID=1000
HOST_USER=ros
HOST_GROUP=ros
