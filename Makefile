# =============================================================================
# Makefile — Build & Run MUTO-RS (ROS2 Control + IMU)
# =============================================================================

DOCKER_COMPOSE = docker compose
SERVICE = muto_rs
ENV_FILE = .env

# ---------------------------------------------------------------------------
# Build l'image Docker avec .env
# ---------------------------------------------------------------------------
build:
	$(DOCKER_COMPOSE) --env-file $(ENV_FILE) build $(SERVICE)

# ---------------------------------------------------------------------------
# Lancer le container en arrière-plan
# ---------------------------------------------------------------------------
up:
	$(DOCKER_COMPOSE) --env-file $(ENV_FILE) up -d $(SERVICE)

# ---------------------------------------------------------------------------
# Arrêter le container
# ---------------------------------------------------------------------------
down:
	$(DOCKER_COMPOSE) --env-file $(ENV_FILE) down

# ---------------------------------------------------------------------------
# Rebuild + relance
# ---------------------------------------------------------------------------
reup: down build up

# ---------------------------------------------------------------------------
# Logs du container
# ---------------------------------------------------------------------------
logs:
	$(DOCKER_COMPOSE) --env-file $(ENV_FILE) logs -f $(SERVICE)

# ---------------------------------------------------------------------------
# Shell interactif dans le container
# ---------------------------------------------------------------------------
exec:
	$(DOCKER_COMPOSE) --env-file $(ENV_FILE) exec $(SERVICE) bash

# ---------------------------------------------------------------------------
# Nettoyage complet (containers + volumes)
# ---------------------------------------------------------------------------
clean:
	$(DOCKER_COMPOSE) --env-file $(ENV_FILE) down -v --remove-orphans
	docker system prune -f
